function emis = gen_gaussian(vector,verbose)
%GEN_GAUSSIAN Generate a Gaussian distribution
% input: vector - feature vector, with [samples features] as dimension
  if verbose
    fprintf('gen_gaussian: Generating a Gaussian distribution...TODO\n');
  end
  emis.mean=1;
  emis.cov=1;
end